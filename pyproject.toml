[project]
name = "cellsemagenticworkflow"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
markers = [
    "unit: fast, isolated tests",
    "integration: real API interactions",
]
